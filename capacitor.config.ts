import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'instaApp1905349',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
